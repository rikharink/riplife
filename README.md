# riplife

```sh
npm install -g riplife
riplife init
# first configure config.json then you can add team members that left with:
riplife add

# build the static pages, output in ./public/
riplife build
```
